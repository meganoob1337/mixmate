import React, { Component } from 'react';

export default class ProfileForm extends Component {
    render() {
        return (
            <React.Fragment>

            </React.Fragment>
        )
    }
}